[package]
name = "c-api"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
name = "rtmp"
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
shiguredo_rtmp = { path = "../../" }

[build-dependencies]
cbindgen = { version = "0.29.2", default-features = false }
