[package]
name = "publish"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
# コマンドライン引数のパースに使用する
noargs = "0.4"
# RTMPS (TLS) 配信に使用する
rustls = {
  version = "0.23",
  default-features = false,
  features = [
    "aws-lc-rs",
    "std",
    "tls12",
  ]
}
# サーバ証明書のプラットフォーム検証に使用する
rustls-platform-verifier = "0.6"
# MP4 ファイルのトラック情報の読み取りに使用する
shiguredo_mp4 = "2026.1"
# RTMP 配信に使用するライブラリ本体
shiguredo_rtmp = { path = "../.." }
# 非同期 I/O とイベントループに使用する
tokio = {
  version = "1.52",
  features = [
    "io-util",
    "macros",
    "net",
    "rt-multi-thread",
    "time",
  ]
}
# rustls と tokio を橋渡しする TLS ストリームに使用する
tokio-rustls = "0.26"
